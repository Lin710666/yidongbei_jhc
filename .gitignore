# 运行期数据：机体记忆、角色卡、语音缓存、上传的背景与 3D 模型。
# 这些是使用者本机的东西，不该进仓库。
data/

# 依赖与构建产物
node_modules/
dist/

# 验收截图（过程产物，十几 MB）
test/shots/

# 第三方角色模型与背景素材：版权不在本项目，改用 tools/获取示例模型.ps1 下载。
# 保留各个 README.md，让克隆下来的人知道去哪拿。
public/models/*
!public/models/README.md
public/models3d/*
!public/models3d/README.md

# 深度模型权重（约 95MB）：由 tools/fetch-depth-model.js 下载，不入库
models/depth-anything-v2-small/
models/
!models/README.md

# 图片转 3D 的 TripoSR 权重（约 1.6GB）：由 tools/fetch-triposr.js 下载
models/triposr/

# 项目内的 Python venv（跑深度与图生 3D 用）：体积大、平台绑定，不入库
tools/py/

# 技能包打包产物
*.skill

# 编辑器与系统
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log

# Python 字节码缓存：每次跑 worker 都会重新生成，属于产物不属于源码
__pycache__/
*.pyc
*.pyo

# 调试用的临时脚本（约定以 .dbg- 开头），不属于交付物
tools/.dbg-*
.dbg-*

# 本机 TTS 的路径记录（由 tools/find-tts.ps1 生成）
qwen-tts-home.txt

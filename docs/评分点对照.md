# 评分点对照说明

> 赛题 **JBGS-2026-06 文旅智能辅助场景基于 AI Coding 或 OpenClaw 引擎规范**
> 评分标准原文：
> （1）场景适配性（30 分）紧密贴合文旅服务实际流程与使用场景　2. 功能实用性（25 分）有效解决行业高频痛点，提升服务效率　3. 部署便捷性（15 分）安装简单、调试快速、环境兼容好　4. 代码规范性（15 分）逻辑清晰、注释完整、符合 OpenClaw 开发规范　5. 异常处理能力（10 分）可应对信息缺失、网络异常、操作错误等情况　6. 文档完整性（5 分）材料齐全、说明清晰、易于复现

---

## 一、场景适配性（30 分）

### 对应赛题要求

> 针对文旅行业景区、酒店、餐饮核心业态中消费体验同质化、客源转化效率低、产品创新迭代慢三大痛点，
> 开发 1-2 项核心文旅智能化场景功能，聚焦游客消费体验升级、文旅营销创收增效、文旅文创创新迭代三大核心场景。

### 本项目的实现

（1）**不做宽，做深**：只做两项核心功能，与赛题"开发 1-2 项核心文旅智能化场景功能"精确对齐。

| 功能 | 对应痛点 | 对应赛题场景 | 实现位置 |
| --- | --- | --- | --- |
| 个性化方案规划 | 消费体验同质化 | 游客消费体验升级 | 「文旅功能」页签 → 方案规划 |
| 文旅营销素材生成 | 客源转化效率低 / 营销获客成本高 | 文旅营销创收增效 | 「文旅功能」页签 → 营销素材 |

（2）**样本库按业态组织，贴合真实服务流程**：`references/` 下按
景区（`destinations.md`）/ 酒店民宿（`hotels.md`）/ 餐饮（`dining.md`）**三类分库**，
覆盖 5 座城市共 **117 条实体**。字段包含门票、时长、最佳季节、适配人群、
价位档、禁忌友好度等真实业务字段。

（3）**交互形态贴合"服务"语义，而不是"填表"语义**：原项目是一个表单页；
本项目改成**虚拟向导**站在页面上。用户像跟导游说话一样提需求，
点词云上的「杭州」「亲子」「忌辣」就能拼出需求，向导会用语音念出方案。
这直接对应赛题"解决传统文旅行业**人工服务刻板**、客群需求适配度低"的表述。

（4）**五业态覆盖**：营销功能支持 景区 / 酒店 / 餐饮 / **文创** / 活动 五类主体，
对应赛题的"文旅文创创新迭代"。

（5）**形象与场景可换，适配不同讲解场合**：内置 **3 套 Live2D 人物形象**
（含 Project AIRI 自带的基础建模 桃瀬ひより 免费版 / Pro 版）与 **12 个背景**
（3 张 AIRI 图片 + 9 个程序化场景：极光 / 波浪 / 樱花 / 星野 / 点纹 /
山水青绿 / 暖金黄昏 / 宣纸留白 / 纯色），并支持上传自己的图。
讲古镇可以配「山水青绿」，做夜间市集推广可以配「暖金黄昏」，
录屏出图可以配「宣纸留白」。同一套系统能贴合不同文旅场景的调性，
而不是无论讲什么都长一个样。
角色卡里也能绑定形象，做"一角色一造型"。

### 指标点

| 指标 | 数值 / 证据 |
| --- | --- |
| 覆盖文旅业态 | 3 个核心业态（景区/酒店/餐饮）+ 5 类营销主体（含文创、活动） |
| 样本库实体数 | 117 条（服务启动时打印，`/api/status` 可查） |
| 覆盖城市 | 5 座（杭州、苏州、成都、丽江、西安） |
| **可选人物形象** | **3 套 Live2D** + **2 套 3D VRM**（共 5 套，跑一次「获取示例模型.bat」即装到本机），另支持上传自有 `.vrm` / `.glb` |
| **可选背景** | **12 个**（3 张 AIRI 图片 + 9 个程序化场景），另支持上传自有图片 |
| **形象动作丰富度** | 春：27 个动作文件 + 8 个表情；桃瀬ひより：10 个动作文件；Mao：7 个动作文件 + 8 个表情；VRM：标准化骨骼，支持视线跟随 / 眨眼 / 口型 |
| **可上传的内容** | 背景图片（.png/.jpg/.webp/.avif）+ 3D 角色模型（.vrm/.glb），全部存本机 |
| 支持的营销平台 | 5 个（朋友圈、小红书、抖音、OTA、公众号），每个平台有独立写法模板 |
| 可交互功能入口 | 59 个词云词条，全部可点击触发 |
| 方案输出字段 | 每天 6 个字段（上午/午餐/下午/晚餐/住宿/交通贴士）+ 总览表 + 费用预估 + 替代方案与避坑 |

---

## 二、功能实用性（25 分）

### 对应赛题要求

> 有效解决行业高频痛点，提升服务效率。

### 本项目的实现

（1）**输出可直接落地，不是空泛套话**：模型被硬性约束只能逐字引用样本库里的
名称（见 `web/lib/wenlv.js` 的 `OUT_RULES.plan` 第 1 条），
每天字段要求"只写名称本身 + 括号补充"，禁止写成一整句话。
实测输出形如：

```
- 上午：西湖（环湖）（免费，断桥、苏堤、音乐喷泉）
- 午餐：知味观（约40元，小笼包、猫耳朵、定胜糕）
- 住宿：青芝坞·山居民宿（约480元/晚，文艺、近浙大、咖啡馆扎堆）
```

（2）**A/B 双版本文案**：营销功能一次输出"理性卖点版 + 感性情绪版"，
每版含标题、正文、话题标签、配图建议、CTA，运营人员可以直接拿去 A/B 投放。

（3）**一键导出**：结果可导出为 Markdown，数据不出本机，可二次编辑。

（4）**千人千面**：由本地大模型生成，不是规则模板拼接。输入相同的需求两次，
输出不会完全一样。

（5）**语音播报**：向导可以用本机 Qwen TTS 把方案念出来，这对应"人工服务刻板"的
痛点，让服务有"人味"。默认音色「小文 · 温柔讲解」是为文旅讲解场景专门定的。

（6）**看景识景**：本机多模态模型可以看懂游客上传的照片，直接给出文旅建议。

### 指标点

| 指标 | 数值 / 证据 |
| --- | --- |
| 单次方案生成时长 | 2 天方案约 163 s（本机 qwen3:8b，CPU/GPU 混合）；流式输出下首字 3~8 s |
| 生成内容结构完整率 | 输出质检逐项核对：天数是否写满、总览表是否有数据、费用预估三列是否齐、章节是否重复 |
| 编造内容拦截 | 7 类输出质检，把未核实项**如实标注**而不是静默放行 |
| 支持天数 | 1~7 天 |
| 营销平台差异 | 5 个平台各有独立文案特征与结构模板 |
| 语音缓存命中 | 同一句话第二次合成直接读本地缓存（响应头 `X-TTS-Cached: 1`） |

---

## 三、部署便捷性（15 分）

### 对应赛题要求

> 安装简单、调试快速、环境兼容好。

### 本项目的实现

（1）**交付物 ② 一键启动、零第三方依赖**：`web/` 下**没有任何 npm 依赖**
（`package.json` 的 `dependencies` 是空的），只用 Node 内置模块。
双击 `start.bat` 即可，无需 `npm install`，无需构建步骤。

（2）**交付物 ① 一键构建**：`airi/一键构建.bat` 或
`pwsh -File scripts/build-airi.ps1`，自动完成
克隆 → 打补丁 → 装依赖 → 构建工作区包 → 构建前端 → 补 Cubism Core，
每步有明确的成功/失败提示，失败即停。

（3）**依赖全部可选、缺失不阻断**：视觉模型、向量模型、语音服务都是可选的，
缺哪个就在界面上显示橙色状态灯并给出**可直接照做的补装命令**。

（4）**构建踩坑已固化进脚本**（这是"环境兼容好"的实际内容）：

| 坑 | 脚本里的处理 |
| --- | --- |
| `sharp@0.29.3` 需要 MSVC C++ 工作负载，无 VS 的 Windows 上必然失败 | 改用 `pnpm install --ignore-scripts`（stage-web 构建不需要它） |
| pnpm 每次运行前会重跑 install，又卡回上一行 | 写 `.npmrc` 的 `verify-deps-before-run=false` |
| AIRI 的 Cubism SDK 下载插件只写 `.cache`，`dist` 里 `Core/` 是空目录 | 构建脚本第 5 步显式兜底复制 |
| 部分包导出 `./dist`，不先构建工作区包就构建前端会失败 | 脚本固定顺序 `build:packages` → `stage-web build` |
| `.bat` 里的中文在 cmd 下乱码/解析崩掉 | 所有 `.bat` 存为 **GBK/936**，开头 `chcp 936 >nul`，用 `ping` 代替 `timeout`，用 `goto` 代替括号块 |

（5）**零基础分步教程**：`docs/部署教程.md` 每一步都有 **✅ 预期结果** 自检点，
末尾有 14 条「常见问题排查表」。

### 指标点

| 指标 | 数值 / 证据 |
| --- | --- |
| 交付物② 部署步骤数 | 3 步（装 Node → 装 Ollama 拉模型 → 双击 start.bat） |
| 交付物② 第三方依赖数 | **0** |
| 交付物② 首次可运行时间 | < 5 分钟（模型已存在时 < 1 分钟） |
| 交付物① 一键构建 | 1 次双击完成 5 个步骤，首次 10~25 分钟（含克隆完整 AIRI 仓库） |
| 跨平台 | Node 代码全平台可跑；`.bat` 面向 Windows，PowerShell 脚本可跨平台 |
| 文档自检点 | 部署教程含 24 个 ✅ 预期结果节点 + 14 行排查表 |

---

## 四、代码规范性（15 分）

### 对应赛题要求

> 逻辑清晰、注释完整、符合 OpenClaw 开发规范。

### 本项目的实现

（1）**模块边界清晰**：`web/lib/` 下每个文件一个明确职责
（ollama / memory / tts / cards / wenlv / audit），
`server.js` 只做路由与编排。

（2）**注释解释"为什么"，而不是复述代码**。举几个例子：

- `web/lib/ollama.js` 的 `THINK` 常量：说明了为什么默认关思考链
  （三条实测原因：耗时、挤占输出预算导致方案写不满、流式时思维链糊界面）。
- `web/lib/memory.js` 的 `tokenize`：说明了为什么中文必须用字符二元组
  （中文没有空格分词，按英文切词会让检索必然失效）。
- `web/lib/tts.js` 的 `DEFAULT_VOICE`：说明了为什么给完整 instruct 而不只给名字。
- `web/server.js` 的 `serveStatic`：说明了目录穿越检查为什么必须带分隔符边界。
- `web/public/css/airi.css` 顶部：逐项标注设计令牌对应 AIRI 的哪个文件。

（3**）符合 AgentSkill / OpenClaw 规范**：`.agents/skills/wenlv-assistant/`
保留完整技能包结构

| 文件 / 目录 | 规范地位 |
| --- | --- |
| `SKILL.md` | 必需。技能身份证（frontmatter 元数据 + 执行指令） |
| `manifest.json` | 权限声明 |
| `references/` | 知识库（景区/酒店/餐饮 + 营销方法论） |
| `scripts/` | 确定性辅助脚本（合规校验、打包、生成入口） |
| `license.txt` | 技能包许可与数据来源 |

并且**已在真实 OpenClaw 引擎中实测通过**（原项目的验证报告保留在
`docs/OpenClaw-引擎集成验证报告.md`，含原始命令回显与踩坑记录）。

（4）**AIRI 侧改动遵守 AIRI 自己的仓库规范**：kebab-case 文件名、
优先 UnoCSS 而非 Tailwind、i18n 文案放在 `packages/i18n`、
不引入无用依赖、幂等补丁脚本而非手工改文件。

（5）**安全编码**：
- 前端 Markdown 渲染**先转义再解析**，杜绝 XSS；
- 静态服务防目录穿越（带分隔符边界比对）；
- `data/` 目录显式 403，不会被当静态文件下载；
- 输出质检告警用 DOM 构建而非 `innerHTML` 拼接用户/模型内容。

（6）**可测**：输出质检逻辑独立成 `web/lib/audit.js`，
原项目的 30 项回归测试（含两次真实翻车输出）随包保留。

### 指标点

| 指标 | 数值 / 证据 |
| --- | --- |
| web 端源码文件数 | 11 个（server.js + 6 个 lib + 4 个 public/js） |
| 中文注释密度 | 每个模块顶部有职责说明；每个非显然分支有原因注释 |
| 第三方依赖 | web 端 0 个；airi 补丁不新增任何依赖 |
| Skill 规范符合 | `SKILL.md` + `manifest.json` + `references/` + `scripts/` + `license.txt` 齐备 |
| 幂等性 | `apply-patch.mjs` 重复执行不产生重复内容 |
| 目录穿越防护 | 已实现并实测（`/../server.js` 被拒） |

---

## 五、异常处理能力（10 分）

### 对应赛题要求

> 可应对信息缺失、网络异常、操作错误等情况。

### 本项目的实现

（1）**信息缺失：一次性集中追问（带可点选选项）**。
前端有专门入口：词云上的「让 AI 先问我」，**不填任何需求直接点**，
服务端走 `type=intake` 分支，只把 `SKILL.md`「2.2 需求采集」的规则交给模型，
让它按规范输出**一次性集中追问**，每个字段都带可点选候选。
这把"信息缺失处理"从文档里的一句话变成**可见的模型行为**。

（2）**目的地不在样本库：降级但不装懂**。
`web/lib/wenlv.js` 的 `sliceCitySection` 找不到目标城市时，
注入的是明确指令："退回通用文旅知识生成，并在结果开头标注
『本地样本库暂无该目的地，以下为通用建议，建议补充本地数据』"。
演示方法：把目的地改成杭州以外的任意城市。

（3）**模型编造内容：输出质检拦下并标注**。
7 类自动核对（详见 `docs/架构说明.md`）：

| 质检项 | 判定方式 |
| --- | --- |
| 推荐项是否来自样本库 | 三类分库白名单逐个核对 |
| 条目类别是否用对 | 午餐/晚餐只允许餐饮库，住宿只允许酒店库，上午/下午只允许景区库 |
| 天数是否写满 | 请求 N 天却只有 M 段 `## Day` |
| 总览表是否有数据 | 数据行数为 0 |
| 字段是否被占位符敷衍 | 统计「无 / 待定」与整段缺失比例 |
| 章节是否重复 | `##` 级标题是否重名 |
| 表格列数是否与表头一致 | 逐表比对 |

（4）**系统级异常：按类型返回不同状态码，不一律 200**

| 异常 | HTTP | 前端表现 |
| --- | --- | --- |
| Ollama 未安装/未运行 | 502 | 状态灯变红，显示安装/启动命令 |
| 模型未下载 | 503 | 状态灯变橙，显示 `ollama pull xxx` |
| 生成超时 | 504 | 提示已等待秒数 + 建议先 `ollama run` 预热 |
| 未启动本地服务 | — | 状态灯变灰，提示"请双击 start.bat" |
| 模型返回为空 | 502 | 提示重试 |
| 畸形 URL（如 `/%`） | 400 | **服务保持存活**（原项目旧版会直接崩服） |
| 请求体非法 JSON / 非法 type / 过大 | 400 / 400 / 413 | 明确文案 |
| 图片过大 | 413 | 前端先降采样；后端再校验 |
| 端口被占用 | — | 友好提示 + 换端口建议 |
| 语音服务未启动 | 502 | 明确说明如何以 `--api` 启动 Qwen TTS |
| 视觉模型未安装 | 503 | 明确给出 `ollama pull qwen2.5vl:7b` 等三个候选 |
| 记忆文件损坏 | — | 备份为 `memory.corrupt.json` 后从空记忆继续，**服务不崩** |
| 角色卡文件损坏 | — | 备份后恢复内置角色卡，**服务不崩** |

（5）**操作错误防护**：
- 生成期间全局禁用按钮（本机显存有限，重复点击会互相拖慢）；
- 提供「停止」按钮可中断流式生成；
- 输入矛盾（如预算与需求冲突）由 `SKILL.md`「四、异常处理」要求模型指出冲突点并给两套方案；
- 越界/无关请求、危险内容：`SKILL.md` 要求礼貌说明范围 / 拒绝生成并说明理由。

### 指标点

| 指标 | 数值 / 证据 |
| --- | --- |
| 覆盖的异常类型 | 13 类系统级 + 6 类模型级 |
| 错误码到状态码的映射 | 12 个业务错误码 → 400/413/502/503/504/500 |
| 现场可演示的异常处理 | 2 个（「让 AI 先问我」按钮 + 改城市触发降级） |
| 输出质检项数 | 7 类 |
| 进程存活保证 | 所有请求在 try/catch 内；`unhandledRejection` / `uncaughtException` 记日志不退出 |

---

## 六、文档完整性（5 分）

### 对应赛题要求

> 材料齐全、说明清晰、易于复现。

### 本项目的实现

| 文档 | 内容 |
| --- | --- |
| `README.md` | 总览：两套交付物、做了什么、快速开始、目录结构、端口表、设计取舍 |
| `docs/部署教程.md` | 零基础分步部署，每步含 ✅ 预期结果，末尾 14 条排查表 |
| `docs/架构说明.md` | 架构图、数据流、前端/后端结构、算法说明、实测性能数据 |
| `docs/评分点对照.md` | 本文件 |
| `docs/演示脚本.md` | 录演示视频的分镜脚本（含旁白） |
| `web/README.md` | 网页版说明 |
| `airi/README.md` | AIRI 改进版说明（含改了什么、为什么这么改） |
| `web/lib/*.js` 头注释 | 每个模块的职责与设计原因 |
| `.agents/skills/wenlv-assistant/SKILL.md` | 技能规范文档（引擎可加载） |
| `docs/OpenClaw-引擎集成验证报告.md` | 引擎集成实测记录（含原始命令回显） |

### 指标点

| 指标 | 数值 |
| --- | --- |
| 文档总数 | 10 份（含 Skill 规范文档） |
| 部署教程自检点 | 24 个 |
| 排查表条目 | 14 条 |
| 一键脚本 | 5 个（web 的 start/stop，airi 的 一键构建/启动/停止） |

---

## 七、赛题硬性要求的逐条核对

| 赛题要求 | 是否满足 | 实现位置 |
| --- | --- | --- |
| 基于 AI Coding 或 OpenClaw 引擎规范 | ✅ | 开发过程见 git 历史；技能包符合 AgentSkill/OpenClaw 规范并已实测装载 |
| 开发 1-2 项核心文旅智能化场景功能 | ✅ | 2 项：个性化方案规划、文旅营销素材生成 |
| 支持本地部署 | ✅ | 全部服务本机运行，默认只监听 127.0.0.1 |
| 支持数据安全导出 | ✅ | 结果一键导出 Markdown；`data/` 均可直接删除/备份 |
| 界面简洁直观、操作零门槛 | ✅ | 词云点击即用，无需理解表单；六页签信息分层 |
| 覆盖景区/酒店/餐饮核心业态 | ✅ | 三类分库样本库 + 营销支持五类主体 |
| 聚焦游客体验升级 / 营销创收增效 / 文创创新迭代 | ✅ | 方案规划 → 体验升级；营销素材 → 创收增效；文创为营销五主体之一 |
| 提交完整源代码 | ✅ | `web/` + `airi/`（含补丁与桥） |
| 提交一键部署教程 | ✅ | `docs/部署教程.md` + 5 个一键脚本 |
| 提交功能演示视频 | 见 `docs/演示脚本.md` | 需按脚本自行录制 |
| 功能运行稳定 | ✅ | 13 类异常处理 + 进程级兜底 + 服务实测存活 |
| **不得出现参赛单位信息** | ✅ | 全部源码、文档、界面中**不含任何单位名称**（已用关键词全量扫描核查） |

---

## 八、已知的局限

不想夸大成"什么都能做"。以下都是明知而未在本版解决的问题：

| 局限 | 现状 | 影响 |
| --- | --- | --- |
| 非流式接口较慢 | `/api/quick-plan` 端到端约 163 s（2 天方案） | 页面走的是**流式**接口，首字 3~8 s，体感可接受；纯文本接口主要给 Agent 用 |
| 长天数方案仍可能写不满 | 7 天方案在 7B 级模型上仍可能提前收尾（实测 5~6 天） | 已由输出质检主动告警"请求 N 天、实际只生成 M 天"；2~3 天方案稳定写满 |
| 输出质检是"检查"而非"纠正" | 只做白名单核对与结构检查，不修改模型输出 | 能发现并标注编造内容，但不能阻止模型生成它；根治需要更强约束或更大模型 |
| 无并发控制 | 多个请求同时打到 Ollama 会互相拖慢 | 前端已用"生成期间禁用按钮"缓解 |
| 单轮生成（方案接口） | 文旅生成接口每次请求独立 | 不支持"把 Day 3 换个餐厅"这类追问式修改；**对话页签**是带上下文的，可以在那里追问 |
| 样本库为示例数据 | `references/` 按城市分节，5 城数据为公开常识整理（票价标「约」） | 正式使用需替换为本地真实业态数据；服务端只注入所选城市那一节，扩充城市不挤占上下文 |
| 本机显存吃紧 | qwen3:8b 与 Qwen TTS 1.7B 同时加载时显存紧张 | 建议把 Ollama 与 TTS 错峰使用，或换更小的模型（`qwen2.5:3b` / `0.6B-CustomVoice`） |
| AIRI 侧未接长期记忆的自动写入 | 提供了 `/memory/*` 接口，但 AIRI 的对话链路没有默认调用它 | 需要在 AIRI 侧再接一层（属于 AIRI 本体改造，超出本次范围） |

# 文旅智能辅助 v4.0

一个跑在本机的文旅智能助手。左边是 Live2D 或 3D 虚拟向导，右边是对话与功能面板，
所有能做的事都做成了环绕在人物周围的词，点一下就执行。

v4.0 主要动的是词云和结果展示，交互方式跟 v3.1 比变化比较大：

- 词云默认只留骨架。分组标签和核心功能词先露出来，具体条件词（城市、预算、同行人群这些）
  要鼠标进舞台、或者点开某一组，才淡入显示，不会一上来就被几十个词糊住。
- 条件词改成"选中但不立刻生成"。点一下选中、用红框圈出来，再点一下取消，要出东西得再点
  「个性化方案」或「营销文案」。以前是点一个就跑一次，根本凑不出「杭州 + 舒适 + 亲子」这种组合。
  多选只对「兴趣」组开放（和右侧表单里的兴趣一样是多选），城市、预算、人群、忌口这些本来就互斥。
- 词云字色能自己定。以前只有跟随背景亮度自动切黑字/白字，现在可以手动锁死，
  另外给了 8 个常用色的色板和系统取色器。整套配色都从 `--wc-ink` 一个变量推导，改一处全变。
- 全屏状态下有了结果卡。点核心词之后词云收起，网页底部浮出一张占四分之一高度的半透明卡片，
  左上角写角色名，右上角是朗读按钮。行程方案里 Day 1 / Day 2 并排成一行，其余各块各占整行；
  营销文案的版本 A / 版本 B 分成两列，纵向不用滚那么久。
- 「游玩天数」变成了词云上的控件。词本身带上下箭头，点一下加减一天，不用再去右边面板改数字。
- 修了几个实在的问题：全屏词云时背景露黑底（背景模块没监听容器尺寸变化，全屏不触发窗口 resize）；
  想从「杭州」改点「苏州」却点不中（字幕条每说一句话都会把词云整体重排一遍，词跑掉了）；
  大屏上背景粒子被数量上限截顶，看着比小屏还稀。

主要特点：

- **界面**：虚拟人物站在中间，交互词云环绕在两侧；顶部一条工具栏，底部一条字幕条。
- **服务全部本地**：对话、视觉、向量走本机 Ollama；语音走本机 Qwen TTS；
  记忆和角色卡是本机文件。不调用任何云端接口，也不需要 API Key。
- **零第三方依赖**：只用 Node 内置模块，不需要 `npm install`，没有构建步骤。
- **可换外观**：12 个内置背景、5 套人物形象（3 套 Live2D + 2 套 3D VRM），都支持换成自己的。

---

## 快速开始

### 第一次用：先补装依赖

双击 **`检测并安装依赖.bat`**。它会逐项检查 Node.js、Ollama、三个本地大模型、
Qwen TTS WebUI、语音模型和前端素材，把缺的列出来，确认后自动下载。
已经装好的会跳过，不会重复下；什么都没缺就直接告诉你"可以启动了"。

### 日常使用：一键启动

双击 **`start.bat`**。它会依次把下面三个都拉起来，最后自动打开浏览器：

| 顺序 | 启动什么 | 窗口标题 |
| --- | --- | --- |
| 1 | 本机 Ollama（对话 / 视觉 / 记忆向量），没运行会自动起 | —— 托盘运行 |
| 2 | 本机 Qwen TTS 语音合成，没运行会自动起 | `文旅-QwenTTS语音服务` |
| 3 | 文旅智能辅助网页服务 | `文旅智能辅助-网页服务` |

然后打开 <http://localhost:8000>。

### 关掉：一键停止

双击 **`stop.bat`**。它会关掉网页服务和语音服务（含它们占用的端口和窗口），
**但按要求保留 Ollama 不动**，结束时还会复查一遍端口有没有真的释放。

**零第三方依赖**：`package.json` 的 `dependencies` 是空的，所以 `检测并安装依赖.bat` 里
没有任何 npm 相关步骤。

### Qwen TTS 装在别的盘？

它是个独立安装的第三方程序，位置因人而异，`start.bat` / `stop.bat` 会自动去找
（常见位置：桌面、各盘根目录、`<盘>:\声音` 等）。找不到时可以手动告诉它，二选一：

1. 在项目根目录建一个 `qwen-tts-home.txt`，里面写一行安装路径；
2. 或设环境变量 `QWEN_TTS_HOME` 指向该路径。

没装 Qwen TTS 也能正常跑，只是虚拟人物不会出声，其它功能不受影响。

---

## 界面构成

```
┌──────────────────────────────────────────────────────────────────┐
│ 顶栏：品牌 · 状态灯（模型/记忆/语音/视觉）· 朗读开关 · 刷新          │
├────────────────────────────────┬─────────────────────────────────┤
│ 舞台顶部工具条                  │  右侧玻璃面板（6 页签）           │
│  ├ 角色铭牌                     │   💬 对话                        │
│  └ 🖼️背景 · 🧍形象 · ☁️词云 · 🔀重排 · ⤢全屏                      │
│                                │   🧭 文旅（方案/营销/导出）        │
│  Live2D 舞台                    │   🎨 外观（背景/形象/词云）        │
│  ├ 背景层（图片或程序化 Canvas）  │   🧠 记忆（检索/增删/清空）        │
│  ├ 人物（视线跟随/眨眼/口型）     │   🎭 角色卡（增删改/导入导出）     │
│  └ 交互词云（环心留空给人物）     │   🔊 声音（5 音色/试听/语气指令）  │
│                                │                                 │
│ 舞台底部字幕条（角色说的话）      │                                 │
├────────────────────────────────┴─────────────────────────────────┤
│ 输入区：🖼️ 上传图片 · 📷 摄像头 · 提示 · 停止 · 发送                  │
└──────────────────────────────────────────────────────────────────┘
```

### 三层浮层怎么避开彼此

舞台上有三层会浮动的东西：顶部工具条、底部字幕条、词云。
做法是把工具条与字幕条的实测高度当作「安全边距」传给词云布局：

```js
// app.js 的 syncCloudInsets()：量出真实高度
cloud.setInsets({ top: 工具条高 + 34, bottom: 字幕条高 + 30, left: 16, right: 16 });
```

词云的排布边界改用「可用区」（容器减去安全边距），于是三者不会互相遮挡。
字幕条出现/消失时会重新量一次（只在**可见性变化**时重排，不是每个流式片段都重排）。

> 有个容易写错的细节：**人物排除椭圆的圆心必须用「舞台中心」，不能用「可用区中心」**。
> 因为 Live2D 人物站在舞台正中，而"可用区"会被工具栏和字幕条挤得整体上移；
> 两者一旦共用一个圆心，字幕条一出现排除区就跟着往上跑，人物下半身就漏出来被词压住。
> 这个 bug 是被验收脚本里那条「词云不压住人物」抓出来的。

---

## 外观：换背景 / 换形象

点舞台工具条上的「🖼️ 背景」或「🧍 形象」，或进右侧「🎨 外观」页签。

### 背景（12 个内置 + 自定义上传）

| 类别 | 内容 |
| --- | --- |
| **内置图片（3 张，来自 Project AIRI）** | 温馨茶室 · 粉色直播间 · 精灵森林 |
| **程序化背景（9 个，Canvas 现画）** | 极光渐变 · 动态波浪 · 樱花飘落 · 夜空星野 · 十字点纹 · 山水青绿 · 暖金黄昏 · 宣纸留白 · 纯色（跟随主题） |
| **自定义上传** | 你自己的图片，存到 `data/backgrounds/`，可列出、可删除 |

程序化背景是照着 AIRI 的 `Backgrounds` 组件重写的（`part-animated-wave` → 动态波浪、
`SakuraPetal` → 樱花飘落、`pattern-cross` → 十字点纹、默认色相流动 → 极光渐变），
好处是不用带任何图片素材，放到多大都不糊。

「极光渐变」会**跟随角色卡主色**变色；其余背景保留自己的配色
（樱花被染成绿色、山水被染成粉色都不好看）。

选择器缩略图有两种画法：图片用 `drawImage` 裁切，程序化背景调用
`BackgroundManager.renderStatic()` **只画一帧**（`t = 0.9`，让波浪/极光停在有内容的位置），
一次显示 12 个背景，如果每个都跑动画，GPU 会被白白吃掉。

### 人物形象（3 套）

| 模型 | 来源 | 动作 | 表情 | 口型 |
| --- | --- | --- | --- | --- |
| 桃瀬ひより | Live2D 官方示例 | 10 个动作文件 | 0 | 支持 |
| 春 | Live2D 官方示例 | 27 个动作文件 | 8 | 支持 |
| Mao | Live2D 官方示例 | 7 个动作文件 | 8 | 支持 |

三套都支持口型同步（`ParamMouthOpenY`）。

> 模型是第三方素材，**没有随仓库分发**。首次运行先双击
> `tools\获取示例模型.bat`（约 33MB，走 jsDelivr），它会装好上面三套 Live2D
> 和两套 VRM。目录约定与换模型的办法见 `public/models/README.md`。

预览图不是占位图，是离线渲染出来的真实帧：

```powershell
# 用真实渲染器（PixiJS + Cubism）逐个渲染一帧，存成 models/<id>/preview.png
node test/generate-model-previews.mjs
```

之所以离线生成而不是运行时实时渲染：Live2D 的纹理单张就有 2~3 MB，
让前端为每个模型都起一个渲染器太贵。生成一次、静态复用，选择器就只是贴图，零开销。

选中的形象会**写进当前角色卡**，所以切换角色卡时形象跟着换（角色卡里同时存着 `kind`，才知道该用哪套渲染器）。

### 3D 人物形象（VRM / GLB）

| 格式 | 说明 | 能做哪些互动 |
| --- | --- | --- |
| **.vrm** | VRoid / VRM 虚拟形象，带标准化骨骼与表情 | 视线跟随、自动眨眼、说话口型、自然站姿、点头 |
| **.glb** | glTF 二进制单文件 | 整体显示与待机摆动（没有骨骼就没有口型/眨眼） |

内置两套来自 **Project AIRI** 的 VRM 示例模型；也可以**上传自己的模型**。

关于 .gltf：那个格式通常还要带一堆散装 `.bin` 与贴图，
网页上「上传一个文件」这个交互没法把整包带上来。与其让用户传完发现缺贴图，
不如在界面上直接说明「请导出成 .glb 或 .vrm」。

上传校验看的是文件内容，不看扩展名。VRM 与 GLB 本质都是 glTF 二进制，
文件头必须以 `glTF` 开头，扩展名写成 `.vrm` 但内容是别的照样会被拒，
界面会提示「文件头应当是 glTF」，免得改个后缀就写进去一个坏文件。

上传模型的预览图是自动生成的。用户上传的模型只在用户机器上，服务端没有渲染器，
但前端此刻已经把它渲染出来了，所以切过去之后直接截当前这一帧回传落盘
（`data/models3d-previews/`），比再跑一遍离屏渲染省事，截到的也正是用户真实看到的样子。
内置的两套可以一次刷完：

```powershell
node test/gen-3d-previews.mjs
```

T-pose 会掰成自然站姿。VRM 文件的静止姿态就是双臂平举的 T-pose（建模约定），
不加载动画时人物会一直张着手站着。代码用 three-vrm 的 normalized bone
把上臂旋转约 79° 放下，只动上臂，不动小臂与手腕——不同模型的骨骼朝向约定不统一，
猜错轴会把手拧到背后。

three.js 按需加载。three.js 1.3MB 加 three-vrm 0.9MB 一共 2.2MB，
只有真的选了 3D 形象才会 `await import('/js/stage3d.js')`，
默认用 Live2D 的用户完全不会下这两个包。

---

## 交互词云说明

词云一共 64 个词条，分 14 组，全部来自服务端 `lib/wenlv.js` 的 `WORD_CLOUD`。
其中 11 个是分组标签（点它只展开该组，不触发动作），53 个是真正带动作的功能词：

| 分组 | 词条示例 | 点击效果 |
| --- | --- | --- |
| 核心功能 | 个性化方案、营销文案 | 用已选中的条件立即生成 |
| 异常处理 | 让 AI 先问我 | 信息不全时集中追问 |
| 可信度 | 输出质检 | 对照本地样本库检查输出 |
| 目的地 | 杭州、苏州、成都、丽江、西安 | 选中该城市（单选） |
| 行程 | 行程规划、游玩天数、费用预估、避坑提示、替代方案、一键导出 | 「游玩天数」是带上下箭头的控件，其余按预设参数生成或导出 |
| 预算 | 经济、舒适、高端 | 选中该档位（单选） |
| 同行人群 | 亲子、情侣、带老人、团建 | 选中该人群（单选） |
| 兴趣 | 自然风光、人文历史、美食、文艺打卡、购物 | 选中该兴趣（可多选） |
| 饮食禁忌 | 素食、忌辣、清真 | 选中该禁忌（单选） |
| 营销平台 | 小红书、朋友圈、抖音、OTA、公众号 | 选中该平台（单选） |
| 营销产品 | 景区、酒店民宿、餐饮、文创创意、活动策划 | 选中该主体（单选） |
| 文案风格 | 种草、促销、文艺 | 选中该风格（单选） |
| 本地服务 | 机体记忆、记住这个、视觉理解、拍照识景、语音播报、声音设置、角色卡、本地模型 | 打开对应面板 / 存记忆 / 选图 / 朗读 |
| 形象 | 换表情、打招呼 | 轮换 Live2D 表情 / 角色打招呼 |

条件词（城市、预算、兴趣这些）只负责改参数，不会立刻生成，选中的词用红框圈出来，
点同一个词取消；要出东西得再点「个性化方案」或「营销文案」。
多选只对「兴趣」组开放（和右侧表单里的兴趣 chip 一致），城市、预算、人群、忌口、
平台、产品、风格都是单选互斥。

词云和右侧表单用的是同一份选项常量（`lib/wenlv.js` 的 `OPTIONS` / `DEFAULTS`），
所以两边不会对不上。

点词条时角色会随口应一句（比如点「杭州」会说「杭州的安排交给我，正在翻本地样本库…」），
让点击有即时反馈。

---

## 目录结构

```
./
├── server.js                 # 本地服务入口（路由 + 编排，零依赖）
├── lib/
│   ├── ollama.js             # 本机 Ollama：对话 / 视觉 / 向量，自动挑选本机已有模型
│   ├── memory.js             # 机体记忆：本地文件 + 混合检索（语义 + 中文词法）
│   ├── tts.js                # 本机 Qwen TTS 客户端 + 5 个音色预设 + 语音缓存
│   ├── cards.js              # AI 角色卡：CRUD + 导入导出（兼容 SillyTavern 卡）
│   ├── wenlv.js              # 文旅 Skill 引擎：提示词 / 样本库切片 / 质检 / 词云清单
│   ├── backgrounds.js        # 背景清单：内置图片 + 程序化 Canvas 背景
│   ├── models3d.js           # 3D 模型（VRM / GLB）：校验 / 存储 / 预览图
│   ├── voices.js             # 「我的音色」：参考音频上传与声音克隆
│   └── audit.js              # 输出质检（沿用原项目，含 30 项回归测试）
├── public/
│   ├── index.html            # 页面骨架
│   ├── css/airi.css          # 设计令牌与全部样式（对齐 AIRI）
│   ├── js/
│   │   ├── util.js           # DOM / Markdown（防 XSS）/ SSE / 图片降采样
│   │   ├── backgrounds.js    # 背景管理器（内置图片 / 程序化 / 纯色）
│   │   ├── live2d.js         # Live2D 舞台（视线跟随 / 嘴型同步 / 表情 / 动作）
│   │   ├── wordcloud.js      # 词云（螺线布局 + 碰撞检测 + 点击分发）
│   │   ├── stage3d.js        # 3D 舞台（three.js + three-vrm，按需加载）
│   │   └── app.js            # 主程序
│   ├── vendor/               # Live2D 运行时（已本地化，离线可用）
│   └── models/               # Live2D 模型目录（空，跑「获取示例模型.bat」填充）
├── .agents/skills/wenlv-assistant/   # 符合 AgentSkill/OpenClaw 规范的技能包
├── docs/                     # 部署教程 / 架构说明 / 评分点对照 / 验收报告
├── test/
│   ├── audit.js              # 输出质检回归测试（30 项，含两次真实翻车输出）
│   ├── smoke.js              # 冒烟测试（28 项：崩服回归 / 穿越 / data 泄露 / 新接口契约）
│   └── fixtures/             # 回归样本
├── data/                     # 运行期数据（记忆 / 角色卡 / 语音缓存 / 自定义音色），不出本机
├── start.bat                 # 一键启动：Ollama + Qwen TTS + 网页服务 + 开浏览器
├── stop.bat                  # 一键停止：关掉网页与语音服务，保留 Ollama
├── 检测并安装依赖.bat          # 检查并补装所有缺失的依赖与模型
├── tools/
│   ├── env-detect.bat        # 共用：探测 Qwen TTS 装在哪（三个 bat 都调它）
│   ├── find-tts.ps1          # 自动扫描常见安装位置
│   ├── start-tts.bat         # 在新窗口启动 Qwen TTS 语音服务
│   ├── stop-services.ps1     # 按端口 + 命令行特征精确停服务，绝不误伤 Ollama
│   ├── fetch-tts-model.ps1   # 触发 Qwen TTS 下载语音模型
│   └── 获取示例模型.bat/.ps1  # 下载 Live2D 与 3D 示例模型（第三方素材）
└── package.json
```

---

## API 一览

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/status` | 总状态：Ollama / TTS / 记忆 / 角色卡 / 样本库 / Live2D 模型 |
| GET | `/api/capabilities` | 能力清单：选项 + 词云 + 音色 + 模型 + 角色卡 |
| POST | `/api/chat` | **SSE** 流式对话（人设 + 记忆召回 + 可选视觉） |
| POST | `/api/wenlv/generate` | **SSE** 流式方案/文案/追问（含输出质检） |
| POST | `/api/generate` | 非流式生成（保留原项目 JSON 契约，便于自动化与 Agent 调用） |
| GET | `/api/quick-plan` | Agent 友好：纯文本方案（OpenClaw 等可直接转述） |
| GET | `/api/quick-marketing` | Agent 友好：纯文本文案 |
| GET/POST/DELETE | `/api/memory*` | 机体记忆：列表 / 写入 / 检索 / 清空 / 删除 |
| POST | `/api/vision` | 视觉理解（base64 图片 → 中文描述） |
| POST | `/api/tts` | 语音合成（返回 `audio/wav` 二进制）。卡片带 `refVoiceId` 时自动走声音克隆 |
| GET | `/api/tts/status` | 语音服务状态 + 音色预设 |
| GET | `/api/tts/speakers` | 内置说话人清单（9 个，含中文名与实测基频） |
| GET/POST | `/api/voices` | 「我的音色」：列表 / 上传参考音频（`{name, refText, audio}`）→ 存 `data/voice-refs/` |
| GET/PUT/DELETE | `/api/voices/:id` | 试听（`/audio`）/ 改名 / 删除 |
| GET/POST/PUT/DELETE | `/api/cards*` | AI 角色卡：列表 / 新建 / 更新 / 删除 / 复制 / 激活 / 导入 / 导出 |
| GET | `/api/backgrounds` | 背景清单：`{ bundled, procedural, custom }` |
| POST | `/api/backgrounds` | 上传背景（`{image: dataURL, name}`）→ 存 `data/backgrounds/` |
| GET | `/api/backgrounds/file/:name` | 取回某个自定义背景图片 |
| DELETE | `/api/backgrounds/:id` | 删除自定义背景 |
| GET | `/api/models3d` | 3D 模型清单：`{ formats, bundled, custom }` |
| POST | `/api/models3d` | 上传 3D 模型（`{model: base64, name}`）→ 存 `data/models3d/` |
| GET | `/api/models3d/file/:name` | 取回某个上传的 3D 模型 |
| POST | `/api/models3d/preview` | 保存模型预览图（`{url, image}`） |
| GET | `/api/models3d/preview/:key` | 取回预览图 |
| DELETE | `/api/models3d/:id` | 删除上传的 3D 模型 |

### SSE 事件格式

`POST /api/chat` 与 `POST /api/wenlv/generate` 都返回 `text/event-stream`，
每条事件是 `data: {json}\n\n`，末尾 `data: [DONE]\n\n`。

```
{"type":"start","model":"qwen2.5:7b", ...}   # 开始
{"type":"vision","model":"qwen2.5vl:7b","text":"..."}   # 视觉识别结果
{"type":"memory","hits":[...]}               # 召回了哪些长期记忆
{"type":"delta","text":"..."}                # 增量文本（流式）
{"type":"done","content":"...","warnings":[...]}        # 结束 + 质检结果
{"type":"error","code":"NO_MODEL","error":"..."}        # 出错
```

---

## 环境变量

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `PORT` | `8000` | 服务端口 |
| `HOST` | `127.0.0.1` | 监听地址。默认只监听本机，外部连不进来 |
| `DATA_DIR` | `./data` | 数据目录（记忆 / 角色卡 / 语音缓存 / `voice-refs/` 自定义音色） |
| `OLLAMA_URL` | `http://127.0.0.1:11434` | Ollama 地址 |
| `OLLAMA_MODEL` | 自动挑选 | 强制指定对话模型 |
| `OLLAMA_VISION_MODEL` | 自动挑选 | 强制指定视觉模型 |
| `OLLAMA_EMBED_MODEL` | 自动挑选 | 强制指定向量模型 |
| `OLLAMA_TIMEOUT_MS` | `300000` | 单次生成超时 |
| `OLLAMA_THINK` | `0`（关闭） | 设 `1` 开启 `qwen3` 等模型的思考链（会明显变慢） |
| `QWEN_TTS_URL` | `http://127.0.0.1:7860` | 本机 Qwen TTS 地址 |
| `MAX_BODY` | `25165824` | 请求体上限（图片走 base64，所以放宽到 24 MB） |
| `MAX_IMAGE_BYTES` | `12582912` | 单张图片上限 12 MB |

### 关于 `OLLAMA_THINK`

默认是关掉的。原因是实测的三条：一是耗时，本机 CPU/GPU 混合推理，开着思考出一份
2 天行程要 5 分钟以上；二是挤占输出预算，会出现「方案只写了 3 天就收尾」；
三是流式输出时思维链会先糊到界面上。确实需要就设 `OLLAMA_THINK=1`。

---

## 测试

```powershell
npm test                          # 服务端两组一起跑
node test/audit.js                # 输出质检回归（30 项）
node test/smoke.js                # 冒烟测试（28 项）

# 浏览器验收（需要先启动服务 + 系统里有 Edge）
node test/verify-browser.mjs      # 22 项：页面/词云/Live2D/流式生成/页签/异常
node test/verify-look.mjs         # 30 项：界面分层/换背景/换形象/上传背景
node test/verify-3d.mjs           # 23 项：3D 形象渲染/上传 VRM、GLB/非法文件拒绝
node test/gen-3d-previews.mjs     # 给所有 3D 模型重新生成预览图
node test/generate-model-previews.mjs   # 给每个 Live2D 模型重新生成预览图
```

| 测试 | 覆盖 |
| --- | --- |
| `audit.js` | 输出质检的 10 类用例，其中两次是真实翻车输出的回归样本 |
| `smoke.js` | 崩服回归 / 目录穿越 / `data/` 泄露 / 非法参数 / 新接口契约。跑完所有畸形请求之后服务仍然存活 |
| `verify-browser.mjs` | 采样 WebGL 像素确认 Live2D 真的渲染出来了；词云零重叠、不侵入人物；点词云真的触发流式生成 |
| `verify-look.mjs` | 舞台分层正确；词云不压工具栏和字幕条；换背景后比对画面像素确认真的换了；上传背景走完整链路 |
| `verify-3d.mjs` | 3D 画布真的渲染出内容（采样像素 + 量人物在画面里的占比）；上传合法 GLB 走完整链路；上传 PNG 被明确拒绝；两套渲染器能来回切 |

冒烟测试会用自己的临时端口与临时数据目录起一份服务副本，不污染正式数据。

---

## 数据与隐私

- 所有推理在本机：**Ollama**（对话 / 视觉 / 向量）+ **本机 Qwen TTS**（语音）。
  不调用任何云端 API，不需要任何 API Key。
- 所有持久化都在 `data/` 下：`memory.json`（机体记忆）、`cards.json`（角色卡）、
  `tts-cache/*.wav`（语音缓存）。可直接删除或备份带走。
- 服务默认只监听 `127.0.0.1`；`data/` 目录被静态服务显式屏蔽（403）。

---

## 已知限制

见 [`docs/评分点对照.md`](docs/评分点对照.md) 第八节「已知的局限」。
简要版：

| 限制 | 说明 |
| --- | --- |
| 非流式接口要整段返回 | `/api/quick-plan` 实测 17.5 s（2 天方案）；页面走流式接口，首字 0.1~1.2 s |
| 舞台动画会和 Ollama 抢算力 | 实测在软件渲染的浏览器里，同一个生成请求会被拖慢数倍；直连接口 15~25 s。演示前可关掉舞台动画 |
| 长天数方案可能写不满 | 7 天方案在 7B 模型上仍可能提前收尾，已由输出质检主动告警 |
| 输出质检是"检查"而非"纠正" | 能发现并标注编造内容，不能阻止模型生成它 |
| 无并发控制 | 多个请求同时打到 Ollama 会互相拖慢，前端已用"生成期间禁用按钮"缓解 |
| 样本库为示例数据 | `.agents/skills/wenlv-assistant/references/` 为公开常识整理，正式使用需替换为本地真实业态数据 |
| 模型需自行获取 | 仓库不含模型文件（第三方版权）。跑一次「获取示例模型.bat」装好示例模型，或把自己的模型放进 `public/models/` |
